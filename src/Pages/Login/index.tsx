import LoginForm from '@/Components/Form';
import { LoginContainer } from './style';

const LoginPage = () => (
  <LoginContainer>
    <h2>로그인</h2>
    <LoginForm />
  </LoginContainer>
);

export default LoginPage;
