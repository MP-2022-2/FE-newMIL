import LoginForm from '@/Components/Form';
import { LoginContainer } from './style';

const LoginPage = () => (
  <LoginContainer>
    이 페이지는 로그인 페이지입니다
    <LoginForm />
  </LoginContainer>
);

export default LoginPage;
