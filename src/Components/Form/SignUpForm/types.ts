export interface SignUpFormProps {
  select: boolean;
}

export interface SignUpFormValues {
  id: string;
  pw: string;
  email: string;
  pwcheck: string;
  name: string;
  company: string;
  studentId: number;
}
