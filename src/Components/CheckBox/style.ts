import styled from 'styled-components';

export const CheckBoxContainer = styled.div`
  input[type='checkbox'] {
    margin-right: 0.4rem;
    vertical-align: middle;
  }

  input[type='checkbox'],
  label {
    cursor: pointer;
  }
`;
